# RAG Backend Dependencies
# Core API & Transport
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic[email]>=2.5.0
email-validator>=2.0.0
pydantic-settings>=2.1.0
httpx>=0.25.0
python-multipart>=0.0.6

# Auth, Identity, RBAC
argon2-cffi>=23.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Database
sqlmodel>=0.0.14
sqlalchemy[asyncio]>=2.0.23
aiosqlite>=0.19.0
alembic>=1.12.0

# Document Ingestion & Parsing
aiofiles>=23.2.0
pdfplumber>=0.10.0
python-docx>=1.1.0

# Advanced Document Parsing (Phase 8 - Docling + Granite VLM)
docling>=2.67.0
docling-core>=2.0.0
langchain-text-splitters>=0.0.1
langchain-core>=0.1.0

# Embeddings & NLP
sentence-transformers>=2.2.0
torch>=2.1.0
numpy>=1.24.0
# spacy>=3.7.0  # OPTIONAL - Not compatible with Python 3.14+, entity extraction uses LLM instead

# Vector Database (FAISS - works natively on Windows, no Docker needed)
faiss-cpu>=1.7.0

# Hybrid Search & Reranking (Phase 2)
rank-bm25>=0.2.2
tiktoken>=0.5.0

# Rate Limiting
slowapi>=0.1.9

# Graph (Phase 4 - GraphRAG)
networkx>=3.2.0
rustworkx>=0.17.0  # High-performance Rust graph library for GraphRAG

# Redis (optional - for caching and task queue)
redis>=5.0.0

# Background Tasks (optional - for production)
# celery[redis]>=5.3.0
# dramatiq[redis]>=1.14.0

# Monitoring & Logging
prometheus-client>=0.19.0
structlog>=23.2.0

# Development
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Optional: OpenTelemetry (for distributed tracing)
# opentelemetry-sdk>=1.21.0
# opentelemetry-instrumentation-fastapi>=0.42b0
# opentelemetry-exporter-otlp>=1.21.0

# OCR Dependencies (Optional)
pytesseract>=0.3.10
Pillow>=10.0.0
PyMuPDF>=1.23.0  # PDF rendering for OCR (no poppler needed)

# Optional: EasyOCR (GPU-accelerated OCR using existing PyTorch)
easyocr>=1.7.0
# pdf2image>=1.16.0  # Fallback PDF converter (requires poppler-utils installed on system)

# QR/Barcode Scanning
pyzbar>=0.1.9
